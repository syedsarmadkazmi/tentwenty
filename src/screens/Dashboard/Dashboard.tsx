import { View } from "react-native"
import { Typography } from "~components"
import { GStyles } from "~theme"


export function Dashboard() {
	return (
		<View style={GStyles.dummyScreen}>
			<Typography>User Dashboard</Typography>
		</View>
	)
}